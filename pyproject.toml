# SPDX-FileCopyrightText: 2026 Paul Galbraith
# SPDX-License-Identifier: BSD-3-Clause

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "pytest-testaferro"
version = "0.1.0.dev6"
description = "pytest facade for DOS-based CppUTest unit testing, run via QEMU"
readme = "README.md"
authors = [{ name = "Paul Galbraith", email = "paul@galbraiths.ca" }]
requires-python = ">=3.9"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
keywords = ["pytest", "cpputest", "qemu", "dos", "testing", "guest"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: Pytest",
    "Intended Audience :: Developers",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Testing",
]
# pytest is the facade's host surface (testaferro.facade). QemuSuiteBackend
# requires reliquary, which is included as a dependency.
dependencies = ["pytest", "reliquary==0.1.0.dev3"]

[project.urls]
Repository = "https://github.com/ferroteca/testaferro"

# Auto-loading is the point: `pytest tests/SUITE.EXE` works the moment
# the distribution is installed, with no wrapper and no flag in front
# of it (D13). The claiming policy in testaferro/plugin.py is what
# makes that safe — a tree scan claims nothing that was not opted in.
[project.entry-points.pytest11]
testaferro = "testaferro.plugin"

[tool.setuptools]
packages = ["testaferro"]
